import React from "react";
import { StyleSheet, View, Text } from "react-native";

class Instances extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <View style={{ flex: 1 }}>
        <Text>Page des instances</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});

export default Instances;
